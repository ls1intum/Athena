# Integration tests

Executes integration tests for Athena

## Development Setup

1. Install dependencies with poetry:

```
poetry install
```

2. Set the POETRY_PATH environment variable for the tests package.

## Usage

### Start Directly

`poetry run tests_all` from the top-level Athena directory