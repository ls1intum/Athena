import { ModuleMeta } from "@/model/health_response";

export default function TextEvaluation({
  athenaUrl,
  athenaSecret,
  module,
}: {
  athenaUrl: string;
  athenaSecret: string;
  module: ModuleMeta;
}) {
  return (
    <p>TODO</p>
  );
}